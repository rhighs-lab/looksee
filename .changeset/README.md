# Changesets

Run `pnpm changeset` for each user-facing change. Choose a patch, minor, or
major bump and write a short summary. Commit the generated Markdown file with
the change. Changesets combines these entries into a version PR and changelog.

See [Contributing](../CONTRIBUTING.md#releases) for the release workflow and
one-time registry setup.
